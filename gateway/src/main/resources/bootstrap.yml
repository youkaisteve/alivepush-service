spring:
  boot:
    admin:
      url: http://monitor:8080
  application:
    name: gateway
  devtools:
    remote:
      restart:
        enabled: true
  cloud:
    config:
      uri: http://config-server:8040/
      profile: dev
      label: master